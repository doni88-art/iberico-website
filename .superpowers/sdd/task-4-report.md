# Task 4 Report: Hero logo lockup (Option C)

**Status:** COMPLETE
**Branch:** `feat/hero-logo-events-popup`
**Commit:** `31645af4e1add04ecceffbe70e9c7bcd660f2586` — `feat(hero): replace white logo card with crest + wordmark lockup`
**File changed:** `src/components/Hero.tsx` only (10 insertions, 8 deletions)

## Steps performed

1. `git checkout -- src/components/Hero.tsx` — discarded the stale abandoned edit. The
   restored committed version's logo-card block matched the brief's "before" snippet
   exactly.
2. Removed `import { LogoMark } from "./Logo";` (now unused). `Logo.tsx` untouched — no
   `knockout` prop added.
3. Replaced the logo-card `<motion.div>` with the brief's lockup block verbatim:
   `flex flex-col items-center gap-2.5`, knockout crest
   `<Image src="/brand/logo-mark.png" width={462} height={601}>` at
   `h-[52px] w-auto brightness-0 invert drop-shadow-[0_2px_16px_rgba(0,0,0,0.5)]`,
   `<span className="h-px w-9 bg-cream/50" />` hairline, and live-text
   `IBÉRICO` in `font-display text-sm tracking-[0.34em] text-cream sm:text-base`
   with inner `<span className="pl-[0.34em]">`.

## Verification

| Check | Result |
|---|---|
| `npx tsc --noEmit` | PASS (clean, no output) |
| `npm run lint` | PASS (clean, no warnings/errors) |
| `npm run dev` + load `http://localhost:3000` | PASS, hero renders correctly |
| Console errors | Only `_next/hmr` WebSocket failures (Browser-pane HMR, unrelated) |

### Desktop screenshot observations (~1280px pane)
- Hero identity block reads top-to-bottom: eyebrow pill `VINOS Y TAPAS · SAIGON · EST. 2021`
  (uppercased via CSS from `t.hero.eyebrow` = "Vinos y Tapas · Saigon · Est. 2021"),
  then white knockout crest, then a short centered hairline, then letter-spaced
  `IBÉRICO` as live text, then the `Tapas / y Vino` headline.
- **No white box / card** behind the logo — the `bg-cream` panel is gone.
- Crest is knockout-white (`brightness-0 invert`) with a soft drop shadow, sitting
  legibly over the dusk patio photo. Everything centered.
- Entrance animation (fade + slight scale/slide) plays on load.
- DOM check (getBoundingClientRect): eyebrow top ≈ 52px, crest img top ≈ 107px,
  hairline top ≈ 169px, `IBÉRICO` text top ≈ 180px — confirms stacking order and that
  the crest keeps its portrait aspect ratio (rendered ~40×52, not distorted).

Note: the brief's Step 5 says "the lockup sits above the eyebrow pill", but the eyebrow
`<motion.span>` precedes the lockup `<motion.div>` in the JSX (unchanged by this task and
by the brief's before/after), so the eyebrow renders above the lockup. Treated as a
wording slip in the brief; the brief's code was used verbatim.

### 375px width observations
- Viewport emulated to 375×812, page reloaded.
- Eyebrow pill spans near-full width; crest sits below it at the same ~52px height
  (not oversized), hairline and letter-spaced `IBÉRICO` centered beneath.
- Nothing clipped or overflowing; body does not scroll horizontally. Lockup stays
  centered.

### prefers-reduced-motion
- Could not emulate `prefers-reduced-motion: reduce` at runtime: the Claude-in-Chrome
  extension is not connected this session, and the Browser pane exposes no
  rendering-emulation control (no DevTools "Rendering" panel access).
- Verified by code instead: the lockup's `initial` is
  `{ opacity: 0, scale: 0.92, y: shouldReduceMotion ? 0 : 12 }` — the `y` slide is
  gated on `shouldReduceMotion` (→ 0, no slide). The `opacity` fade and `scale: 0.92`
  entrance are ungated; this is identical to the pre-existing card block and to the
  brief's "before" and "after" snippets (used verbatim), so behavior under reduced
  motion is unchanged from what the brief specifies: the lockup fades/scales in with
  no vertical slide.

## Self-review checklist

- [x] No `LogoMark` references remain (import removed, JSX usage removed) — `grep` clean.
- [x] No `logo-wordmark.png` reference remains in `Hero.tsx` — now `/brand/logo-mark.png`.
- [x] Crest uses `brightness-0 invert`.
- [x] Lockup container is `flex flex-col items-center`.
- [x] Entrance animation still gated on `shouldReduceMotion` (the `y` offset).
- [x] `Logo.tsx` untouched; no `knockout` prop added.
- [x] Committed only `src/components/Hero.tsx`; untracked `.MP4`/`.HEIC`/`Iberico/`/
      `menu-extraction-notes.md` left unstaged. `AGENTS.md` was not regenerated by
      `next dev` this run (not present in `git status`).

## Concerns

- Reduced-motion path verified by code, not by live emulation (tooling limitation noted
  above).

---

## Follow-up fix — reorder crest lockup above the eyebrow

**Commit:** `aba11e3947d9e43588e66964da666c594d88d1f9` — `fix(hero): order crest lockup above the eyebrow`
**File changed:** `src/components/Hero.tsx` only (11 insertions, 11 deletions)

The coordinator flagged that the approved Option C mockup puts the crest lockup as the
TOP element. The initial implementation replaced the logo card in place, leaving the
eyebrow pill first. Fixed:

1. Moved the eyebrow `<motion.span>` block to render AFTER the lockup `<motion.div>` and
   before the `<h1>`.
2. Re-cascaded entrance `transition` delays top-down: lockup `delay: 0.05` (kept),
   eyebrow `delay: 0.15` (was default/none), `<h1>` spans nudged to `0.2` / `0.3`
   (were `0.1` / `0.25`) so the headline no longer starts almost simultaneously with
   the eyebrow. Subtitle (0.45) and CTAs (0.6) untouched.
3. Kept the lockup's `mb-8` and the eyebrow's `mb-6`.

### Re-verification

| Check | Result |
|---|---|
| `npx tsc --noEmit` | PASS (clean) |
| `npm run lint` | PASS (clean) |
| dev server reload | PASS |
| Console errors | none (HMR websocket noise only) |

DOM order after fix (getBoundingClientRect tops, desktop): crest img ≈ 55px →
hairline ≈ 120px → `IBÉRICO` text ≈ 127px → eyebrow pill `VINOS Y TAPAS · SAIGON ·
EST. 2021` ≈ 186px → `<h1>` ≈ 236px. Confirmed the fade-in cascades top-down (caught
mid-animation with opacities 0.70 / 0.50 / 0.09 / 0 descending).

**Desktop screenshot:** crest → hairline → letter-spaced `IBÉRICO` → eyebrow pill →
`Tapas / y Vino` headline → subtitle → `Reserve a Table` / `View Menu` buttons →
`SCROLL`. All centered, no white box, nothing else shifted.

**375px screenshot:** same order, eyebrow pill spans near-full width below the lockup,
crest ~52px (not oversized), headline/subtitle/buttons flow below, no clipping, no
horizontal scroll.

Note: the first screenshots after each reload showed the hero faint/blank because the
Framer Motion entrance animation runs slowly in the throttled background Browser pane;
waiting ~5s let it settle. Not a regression — DOM inspection confirmed opacities
climbing to 1.

Reduced motion: still verified by code only (no live emulation available). The eyebrow
and `<h1>` `y` offsets remain gated on `shouldReduceMotion`, unchanged by the reorder.

### Concerns (follow-up)

- None. Both commits touch only `src/components/Hero.tsx`. `AGENTS.md` stayed clean
  (unmodified) across both `next dev` runs — nothing to leave for the controller.
  Working tree otherwise holds only the pre-existing untracked `.MP4` / `.HEIC` /
  `Iberico/` / `menu-extraction-notes.md`.
